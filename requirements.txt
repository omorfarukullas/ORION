# ─── Core ──────────────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# ─── Speech / Audio ────────────────────────────────────────────────────────────
openai-whisper>=20231117          # Local Whisper STT (pulls torch automatically)
sounddevice>=0.4.6                # Mic input / audio stream
soundfile>=0.12.1                 # Audio file I/O for sounddevice
pyttsx3>=2.90                     # Offline TTS
openwakeword>=0.6.0               # Wake-word detection (local)
pyaudio>=0.2.14                   # Required by some openWakeWord internals

# ─── ML / NLP ──────────────────────────────────────────────────────────────────
scikit-learn>=1.3.0               # TF-IDF + Logistic Regression intent classifier
joblib>=1.3.0                     # Model serialization (.pkl)
nltk>=3.8.1                       # Text preprocessing (tokenize, stopwords, stem)

# ─── PC Automation ─────────────────────────────────────────────────────────────
pyautogui>=0.9.54                 # Mouse/keyboard automation + screenshots
psutil>=5.9.5                     # CPU / RAM / battery / process info
Pillow>=10.0.0                    # Image handling (screenshots, pyautogui dep)
pygetwindow>=0.0.9                # Window management (Windows/macOS)
pyperclip>=1.8.2                  # Clipboard operations

# ─── GUI ───────────────────────────────────────────────────────────────────────
customtkinter>=5.2.0              # Modern Tkinter GUI

# ─── Web Dashboard & API ───────────────────────────────────────────────────────
fastapi>=0.100.0                  # Web dashboard backend REST API
uvicorn>=0.23.0                   # ASGI production web server
websockets>=12.0                  # Real-time telemetry & event streaming

# ─── Database ──────────────────────────────────────────────────────────────────
# sqlite3 is part of Python stdlib — no extra package needed

# ─── Utilities ─────────────────────────────────────────────────────────────────
python-dateutil>=2.8.2            # Date/time parsing helpers
requests>=2.31.0                  # Future: external API calls (weather, etc.)
colorlog>=6.7.0                   # Colored console logging

# ─── Dev / Testing ─────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
